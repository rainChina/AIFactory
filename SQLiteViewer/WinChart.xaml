<Window x:Class="SQLiteViewer.WinChart"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SQLiteViewer"
        mc:Ignorable="d"
        Title="WinChart" Height="450" Width="800">
    <Grid>
        <!--<DataGrid x:Name="dataGrid" 
          AutoGenerateColumns="True" 
          IsReadOnly="True" 
          GridLinesVisibility="All" />-->
        <!--<DataGrid x:Name="dataGrid" 
          AutoGenerateColumns="True"
          EnableRowVirtualization="True"
          VirtualizingStackPanel.VirtualizationMode="Recycling"
          ScrollViewer.CanContentScroll="True">
        </DataGrid>-->
        <StackPanel>
            <TextBlock Text="Select Columns to Show:" Margin="10,5"/>
            <WrapPanel x:Name="ColumnTogglePanel" Margin="10,0,10,10" />
            <StackPanel Orientation="Horizontal">
                <Button Click="SelectNone_Click" Content="AllUnchecked"></Button>
                <Button Click="Button_Save_Click" Content="SaveSettings" HorizontalAlignment="Right"></Button>
            </StackPanel>
            <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" EnableRowVirtualization="True" />
        </StackPanel>
    </Grid>
</Window>
