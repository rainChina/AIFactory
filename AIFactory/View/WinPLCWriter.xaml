<hc:Window x:Class="AIFactory.View.WinPLCWriter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIFactory.View"
        xmlns:hc="https://handyorg.github.io/handycontrol" 
        xmlns:viewmodel="clr-namespace:AIFactory.ViewModel"
        mc:Ignorable="d"
        Title="" Height="450" Width="800">
    <hc:Window.DataContext>
        <viewmodel:ViewModelPLCWriter></viewmodel:ViewModelPLCWriter>
    </hc:Window.DataContext>
    <Grid>
        <ListView ItemsSource="{Binding Items}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Name}" Width="100"/>
                        <TextBlock Text="{Binding Id}" Width="100"/>
                        <TextBlock Text="{Binding Value}" Width="100"/>
                        <Button Content="Click" Command="{Binding OnButtonClickCommand}" Style="{StaticResource ButtonPrimary}" 
                                hc:IconElement.Geometry="{StaticResource SearchGeometry}" hc:BorderElement.CornerRadius="10"/>
                        
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</hc:Window>
